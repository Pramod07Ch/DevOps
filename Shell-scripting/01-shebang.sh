#!/bin/bash
# Start of the line in any bash script should always be a SHEBANG  Notation. 

# Apart from the first line, none of the lines are considers as shebang and it should start with #
# SHEBANG : Using this notation, we are going to tell system that the interpretor to be used for this script is bash or python or shell
# Apart from the line 1of the script, anything which starts with `#` will be referred a COMMENT


# How to run a script ?
    # bash scriptName 
    # sh scriptName 
    # ./scriptName
